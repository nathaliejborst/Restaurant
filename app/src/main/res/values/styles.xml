<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/HeaderFont</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.DefaultTextView" parent="android:style/Widget.TextView">
        <item name="android:textColor">#3e5878</item>
        <item name="android:fontFamily">@font/amiko</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="Theme.AppTheme.Light.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar" />

</resources>
